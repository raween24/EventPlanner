import type { Callbacks } from '@langchain/core/callbacks/manager';
import type { BaseChatModel } from '@langchain/core/language_models/chat_models';
import type { BaseMessage } from '@langchain/core/messages';
import type { LangChainTracer } from '@langchain/core/tracers/tracer_langchain';
import type { MemorySaver, StateSnapshot } from '@langchain/langgraph';
import type { SelectedNodeContext } from '@n8n/api-types';
import type { Logger } from '@n8n/backend-common';
import { type INodeTypeDescription, type IRunExecutionData, type ITelemetryTrackProperties, type IWorkflowBase, type NodeExecutionSchema } from 'n8n-workflow';
import type { AssistantHandler } from './assistant';
import type { ResourceLocatorCallback } from './types/callbacks';
import type { HITLInterruptValue, PlanOutput } from './types/planning';
import type { WorkflowState } from './workflow-state';
export type TypedStateSnapshot = Omit<StateSnapshot, 'values'> & {
    values: typeof WorkflowState.State & {
        introspectionEvents?: unknown[];
    };
};
export interface StageLLMs {
    supervisor: BaseChatModel;
    responder: BaseChatModel;
    discovery: BaseChatModel;
    builder: BaseChatModel;
    parameterUpdater: BaseChatModel;
    planner: BaseChatModel;
}
export interface WorkflowBuilderAgentConfig {
    parsedNodeTypes: INodeTypeDescription[];
    stageLLMs: StageLLMs;
    logger?: Logger;
    checkpointer: MemorySaver;
    tracer?: LangChainTracer;
    instanceUrl?: string;
    runMetadata?: Record<string, unknown>;
    featureFlags?: BuilderFeatureFlags;
    onGenerationSuccess?: () => Promise<void>;
    nodeDefinitionDirs?: string[];
    resourceLocatorCallback?: ResourceLocatorCallback;
    onTelemetryEvent?: (event: string, properties: ITelemetryTrackProperties) => void;
    assistantHandler?: AssistantHandler;
}
export interface ExpressionValue {
    expression: string;
    resolvedValue: unknown;
    nodeType?: string;
}
export interface BuilderFeatureFlags {
    templateExamples?: boolean;
    codeBuilder?: boolean;
    pinData?: boolean;
    planMode?: boolean;
    enableIntrospection?: boolean;
    mergeAskBuild?: boolean;
}
export interface ChatPayload {
    id: string;
    message: string;
    workflowContext?: {
        executionSchema?: NodeExecutionSchema[];
        currentWorkflow?: Partial<IWorkflowBase>;
        executionData?: IRunExecutionData['resultData'];
        expressionValues?: Record<string, ExpressionValue[]>;
        valuesExcluded?: boolean;
        pinnedNodes?: string[];
        selectedNodes?: SelectedNodeContext[];
    };
    featureFlags?: BuilderFeatureFlags;
    versionId?: string;
    mode?: 'build' | 'plan';
    resumeData?: unknown;
    resumeInterrupt?: HITLInterruptValue;
    planOutput?: PlanOutput;
}
export declare class WorkflowBuilderAgent {
    private checkpointer;
    private parsedNodeTypes;
    private stageLLMs;
    private logger?;
    private tracer?;
    private instanceUrl?;
    private runMetadata?;
    private onGenerationSuccess?;
    private nodeDefinitionDirs?;
    private resourceLocatorCallback?;
    private onTelemetryEvent?;
    private assistantHandler?;
    private sessionFeatureFlags?;
    constructor(config: WorkflowBuilderAgentConfig);
    private createWorkflow;
    getState(workflowId?: string, userId?: string): Promise<TypedStateSnapshot>;
    private getDefaultWorkflowJSON;
    chat(payload: ChatPayload, userId?: string, abortSignal?: AbortSignal, externalCallbacks?: Callbacks, historicalMessages?: BaseMessage[]): AsyncGenerator<import("./types").StreamOutput, void, unknown>;
    private runCodeWorkflowBuilder;
    private runTriageAgent;
    private drainTriageGenerator;
    private saveTriageOutcome;
    private runMultiAgentSystem;
    private validateMessageLength;
    private setupAgentAndConfigs;
    private createAgentStream;
    private handleStreamError;
    private processAgentStream;
    private handleAgentStreamError;
    private isTokenExpiredError;
    private isLlmQuotaOrRateLimitError;
    private getInvalidRequestError;
}
